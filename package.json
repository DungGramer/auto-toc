{
  "name": "toc",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:babel": "babel toc/toc.js --out-file ./dist/toc.min.js --presets @babel/preset-env,minify --source-type=module --no-comments"
  },
  "devDependencies": {
    "@babel/core": "^7.17.10",
    "@babel/preset-env": "^7.17.10",
    "babel-cli": "^6.26.0",
    "vite": "^2.9.7"
  }
}